import { LESSONS, REGIONS } from './content.js';
import { MEDIA, RESOURCE_SETS, SECTION_MEDIA } from './resources.js';
import {
  calculateProgress, completeLesson, createInitialState, getDueReviews, getLevelSummary, getNextLearningAction, getToday, markAnswer, normalizeState, reviewLesson, shouldRemindToday,
} from './core.js';
import './styles.css';
import './mobile-fix.css';

const STORAGE_KEY = 'nusantara-learning-state-v1';
let state = loadState();
let answerState = null;
const learningChecks = {};

function loadState() {
  try { return normalizeState(JSON.parse(localStorage.getItem(STORAGE_KEY) || '{}')); } catch { return createInitialState(); }
}
function saveState() { localStorage.setItem(STORAGE_KEY, JSON.stringify(state)); }
function escapeHtml(value) { return String(value).replace(/[&<>"']/g, (c) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[c])); }
function getRoute() {
  const parts = window.location.hash.replace(/^#\/?/, '').split('/').filter(Boolean);
  if (parts[0] === 'why-indonesia') return { type: 'why' };
  if (parts[0] === 'dashboard') return { type: 'dashboard' };
  if (parts[0] === 'class' && LESSONS.some((item) => item.id === parts[1])) return { type: 'class', id: parts[1] };
  return { type: 'home' };
}
function withResources(item) { return { ...item, media: MEDIA[item.id], resources: RESOURCE_SETS[item.id], sectionMedia: SECTION_MEDIA[item.id] }; }
function currentLesson() { return withResources(LESSONS.find((item) => item.id === state.currentLessonId) || LESSONS[0]); }
function findLesson(id) { return withResources(LESSONS.find((item) => item.id === id) || LESSONS[0]); }
function completed(id) { return state.completedLessonIds.includes(id); }
function nav() { return `<nav class="app-nav"><a href="#/" class="nav-link">Learn</a><a href="#/why-indonesia" class="nav-link">Why Indonesia?</a><a href="#/dashboard" class="nav-link">My dashboard</a></nav>`; }
function header() { return `<header class="topbar"><a class="brand" href="#/" aria-label="Nusantara Learning home"><span class="brand-mark">✦</span><span><strong>Nusantara</strong><small>Learning journey</small></span></a><div class="top-actions">${nav()}<span class="stat-pill streak">🔥 <b>${state.streak}</b></span><span class="stat-pill xp">✦ <b>${state.xp}</b> XP</span><a href="#/dashboard" class="avatar" aria-label="Open profile">${escapeHtml((state.profile.name || 'FY').slice(0, 2).toUpperCase())}</a></div></header>`; }
function progressBar() { const progress = calculateProgress(state, LESSONS.length); return `<div class="progress-block"><div class="progress-label"><span>Course progress</span><strong>${progress}%</strong></div><div class="progress-track"><span style="width:${progress}%"></span></div><small>${state.completedLessonIds.length} of ${LESSONS.length} classes complete</small></div>`; }
function isUnlocked(id) { const index = LESSONS.findIndex((item) => item.id === id); return index <= 0 || completed(LESSONS[index - 1].id); }
function lessonList() { return `<nav class="lesson-nav" aria-label="Course classes">${LESSONS.map((item) => { const unlocked = isUnlocked(item.id); return `<a class="lesson-nav-item ${item.id === state.currentLessonId ? 'active' : ''} ${completed(item.id) ? 'done' : ''} ${unlocked ? '' : 'locked'}" href="#/class/${item.id}" ${unlocked ? '' : `data-locked-lesson="${item.id}" aria-label="${escapeHtml(item.title)} locked"`}><span class="lesson-number">${completed(item.id) ? '✓' : unlocked ? item.number : '🔒'}</span><span><b>${escapeHtml(item.title)}</b><small>${unlocked ? `${escapeHtml(item.tag)} · ${item.duration}` : 'Complete the previous class first'}</small></span></a>`; }).join('')}</nav>`; }
function sidebar() { return `<aside class="sidebar"><div class="sidebar-intro"><span class="eyebrow">INDONESIA 101</span><h1>Understand the archipelago, one story at a time.</h1><p>Build the map first. Then let history, people, belief, and everyday life connect.</p></div>${progressBar()}${lessonList()}<a class="settings-link" href="#/dashboard">⌁ View profile & history</a></aside>`; }
function shell(body, showSidebar = true) { return `<div class="shell">${header()}<main class="layout ${showSidebar ? '' : 'full-layout'}">${showSidebar ? sidebar() : ''}<section class="content">${body}</section></main><footer><span>Built for a lifelong Indonesia 101 → 201 journey.</span><span>Progress stays on this device.</span></footer></div>`; }
function hero(item) { return `<section class="hero-card" style="--accent:${item.color}"><div class="hero-copy"><div class="eyebrow">CLASS ${item.number} · ${escapeHtml(item.region.toUpperCase())}</div><h2>${escapeHtml(item.title)}</h2><p class="hero-subtitle">${escapeHtml(item.subtitle)}</p><div class="hero-meta"><span>◷ ${item.duration}</span><span>✦ ${item.xp} XP</span><span>▣ ${item.tag}</span></div><a class="primary-button" href="#class-content">${completed(item.id) ? 'Review class' : 'Start class'} <span>→</span></a></div><div class="hero-orbit" aria-hidden="true"><div class="orbit-line"></div><div class="island-shape island-a"></div><div class="island-shape island-b"></div><div class="island-shape island-c"></div><span>INDONESIA</span></div></section>`; }
function media(item) { return `<figure class="lesson-media"><img src="${item.media.image}" data-fallback="${item.media.remoteImage || ''}" alt="${escapeHtml(item.media.imageAlt)}" loading="eager"/><figcaption><b>Why this visual?</b> ${escapeHtml(item.media.purpose)}<br/><small>${escapeHtml(item.media.credit)} · <a href="${item.media.creditUrl}" target="_blank" rel="noreferrer">source and license</a></small></figcaption></figure>`; }
function resources(item) { return `<section class="resources-section"><div class="section-heading compact"><div><span class="eyebrow">OPTIONAL SOURCES</span><h2>Go further if you’re curious</h2></div><span class="section-note">Not required to complete the class</span></div><p class="resources-intro">These are reputable sources for exploring the topic in more depth after the class.</p><div class="resource-grid">${item.resources.map(([kind, title, url, description]) => `<a class="resource-card" href="${url}" target="_blank" rel="noreferrer"><span class="resource-kind">${escapeHtml(kind)}</span><h3>${escapeHtml(title)}</h3><p>${escapeHtml(description)}</p><span class="resource-arrow">Open resource ↗</span></a>`).join('')}</div></section>`; }
function quiz(item) { return `<div class="quiz-card synthesis-quiz" id="quiz-card"><div class="quiz-heading"><span class="quiz-icon">?</span><div><span class="eyebrow">SYNTHESIS CHECK</span><h3>${escapeHtml(item.question)}</h3><p>Bring the whole class together in one answer.</p></div></div><div class="answers">${item.answers.map((answer, index) => `<button class="answer-button" data-answer="${index}">${String.fromCharCode(65 + index)} <span>${escapeHtml(answer)}</span></button>`).join('')}</div><div class="quiz-result" aria-live="polite"></div></div>`; }
function sectionQuiz(section, index) { return `<div class="section-check"><div class="section-check-heading"><span class="check-number">${index + 1}</span><div><span class="eyebrow">CHECK THIS IDEA</span><h4>${escapeHtml(section.question)}</h4></div></div><div class="answers">${section.answers.map((answer, answerIndex) => `<button class="answer-button section-answer" data-section="${index}" data-correct="${section.correct}" data-answer="${answerIndex}">${String.fromCharCode(65 + answerIndex)} <span>${escapeHtml(answer)}</span></button>`).join('')}</div><div class="section-result" aria-live="polite"></div></div>`; }
function vocabPractice(item) { const [word, meaning] = item.vocabulary[0]; const alternatives = item.vocabulary.slice(1, 4).map(([candidate]) => candidate); const choices = [word, ...alternatives]; return `<div class="vocab-practice"><div class="section-check-heading"><span class="check-number">✦</span><div><span class="eyebrow">USE A WORD</span><h4>Which word means “${escapeHtml(meaning)}”?</h4></div></div><div class="answers">${choices.map((choice, index) => `<button class="answer-button vocab-answer" data-vocab-answer="${index}" data-vocab-correct="0">${String.fromCharCode(65 + index)} <span>${escapeHtml(choice)}</span></button>`).join('')}</div><div class="vocab-result" aria-live="polite"></div></div>`; }
function lockedClassPage(id) { const item = findLesson(id); const index = LESSONS.findIndex((lesson) => lesson.id === id); const previous = LESSONS[index - 1]; return shell(`<section class="locked-class"><span class="locked-large">🔒</span><span class="eyebrow">CLASS ${item.number} IS LOCKED</span><h2>${escapeHtml(item.title)}</h2><p>Finish <strong>${escapeHtml(previous.title)}</strong> first so the course can build one idea on top of the next.</p><a class="primary-button" href="#/class/${previous.id}">Go to Class ${previous.number} <span>→</span></a><p class="locked-note">You can return here as soon as that class is complete.</p></section>`, true); }
function classPage(id) {
  const item = findLesson(id);
  if (!isUnlocked(id)) return lockedClassPage(id);
  const sections = item.sections || [];
  const checks = learningChecks[item.id] || { sections: {}, vocab: false, synthesis: false };
  const ready = sections.every((_, index) => checks.sections[index]) && checks.vocab && checks.synthesis;
  return shell(`<div class="class-header"><a href="#/" class="back-link">← All classes</a><details class="mobile-course-menu"><summary>Class ${item.number} of ${LESSONS.length} · Open course map</summary>${lessonList()}</details><span class="class-progress-label">${completed(item.id) ? 'Completed class' : `Class ${item.number} of ${LESSONS.length}`}</span></div>${hero(item)}<section class="class-grid" id="class-content"><div class="lesson-panel"><div class="story-card"><span class="eyebrow">WHAT YOU’LL BE ABLE TO EXPLAIN</span><p>${escapeHtml(item.outcome)}</p><small>${escapeHtml(item.story)}</small></div><div class="lesson-sections"><div class="section-heading compact"><div><span class="eyebrow">READ IN SHORT STEPS</span><h2>${sections.length} ideas to take away</h2></div><span class="section-note">Read → check → continue</span></div>${sections.map((section, index) => { const asset = item.sectionMedia[index]; return `<article class="reading-section"><div class="reading-section-heading"><span class="reading-number">${index + 1}</span><div><span class="eyebrow">${escapeHtml(section.purpose)}</span><h3>${escapeHtml(section.title)}</h3></div></div><figure class="section-media"><img src="${asset[0]}" alt="${escapeHtml(asset[1])}" loading="lazy"/><figcaption><b>${escapeHtml(asset[1])}</b><span>${escapeHtml(asset[2])}</span></figcaption></figure><p class="reading-copy">${escapeHtml(section.reading)}</p>${sectionQuiz(section, index)}</article>`; }).join('')}</div><div class="vocab-section"><div class="subheading"><div><span class="eyebrow">USEFUL LANGUAGE</span><h3>Words from this class</h3></div><span>${item.vocabulary.length} new words</span></div><p class="vocab-intro">These words appear in the readings. First recognize them, then try one in your own note.</p><div class="vocab-grid">${item.vocabulary.map(([word, meaning]) => `<div><strong>${escapeHtml(word)}</strong><span>${escapeHtml(meaning)}</span></div>`).join('')}</div>${vocabPractice(item)}</div>${quiz(item)}<div class="lesson-actions"><button class="primary-button" data-action="complete" ${ready ? '' : 'disabled'}>${completed(item.id) ? 'Completed ✓' : 'Complete class'} <span>+${item.xp} XP</span></button><p class="completion-status" data-completion-status>${ready ? 'All checks complete. You can finish the class.' : `Complete ${sections.length} section checks, the vocabulary check, and the synthesis check to finish.`}</p><textarea data-note placeholder="What is one idea you can now explain?">${escapeHtml(state.notes[item.id] || '')}</textarea></div></div><div class="lesson-aside">${media(item)}<div class="class-side-card"><span class="eyebrow">YOUR NOTES</span><h3>Make this idea yours.</h3><p>Your note is saved on this device and appears in your dashboard history.</p></div></div></section>${resources(item)}`, true);
}
function homePage() {
  const item = currentLesson();
  const nextAction = getNextLearningAction(state, LESSONS, getToday());
  const dueReviewItem = nextAction.type === 'review' ? findLesson(nextAction.lessonId) : null;
  const reviewPrompt = dueReviewItem ? `<section class="review-prompt" aria-label="Spaced review reminder"><div class="review-prompt-icon">↻</div><div><span class="eyebrow">READY TO RECALL</span><h3>${escapeHtml(dueReviewItem.title)}</h3><p>${nextAction.dueCount === 1 ? 'One class is ready for a quick spaced review.' : `${nextAction.dueCount} classes are ready for spaced review.`}</p></div><a class="text-button" href="#/dashboard">Review now <span>→</span></a></section>` : '';
  return shell(`<section class="welcome-strip"><div><span class="eyebrow">WELCOME BACK, ${escapeHtml(state.profile.name.toUpperCase())}</span><h2>Ready to keep exploring?</h2></div><a href="#/dashboard" class="text-button">View my history →</a></section>${hero(item)}${reviewPrompt}<section class="continue-card"><div class="continue-icon">▶</div><div><span class="eyebrow">PICK UP WHERE YOU LEFT OFF</span><h3>${escapeHtml(item.title)}</h3><p>${state.lastStudyDate ? `Last studied ${state.lastStudyDate}` : 'Your first expedition starts here.'}</p></div><a class="text-button" href="#/class/${item.id}">Continue <span>→</span></a></section><section class="section-heading"><div><span class="eyebrow">EXPLORE THE ARCHIPELAGO</span><h2>Indonesia is many worlds</h2></div><span class="section-note">Seven regions · one shared journey</span></section><section class="region-grid">${REGIONS.map((region) => `<article class="region-card" style="--region:${region.color}"><div class="region-art"><span>${region.id === 'bali-nusa' ? '◒' : region.id === 'papua' ? '✺' : '◇'}</span></div><div><h3>${escapeHtml(region.name)}</h3><p>${escapeHtml(region.note)}</p><span class="locked-label">${region.id === 'java' ? 'AVAILABLE NEXT' : 'UNLOCK AS YOU LEARN'}</span></div></article>`).join('')}</section><section class="reminder-card" id="reminder"><div class="reminder-icon">🔔</div><div class="reminder-copy"><span class="eyebrow">KEEP THE THREAD</span><h2>Make Indonesia part of your rhythm.</h2><p>Set a gentle reminder so the app remembers where you left off.</p><div class="reminder-controls"><label><input type="checkbox" data-reminder-toggle ${state.reminderEnabled ? 'checked' : ''}/> Remind me</label><input type="time" data-reminder-time value="${state.reminderTime}" ${state.reminderEnabled ? '' : 'disabled'}/><button class="secondary-button" data-action="notifications">Enable browser notifications</button></div><small class="reminder-status">${state.reminderEnabled ? `Next reminder at ${state.reminderTime}` : 'Reminders are currently off.'}</small></div></section>`, true);
}
function onboardingPage() {
  return `<main class="onboarding-page"><div class="onboarding-mark">✦</div><span class="eyebrow">NUSANTARA LEARNING</span><h1>Understand how Indonesia became many worlds—and one country.</h1><p class="onboarding-lede">Indonesia is not just a list of islands or cultural facts. Learn how geography, migration, trade, belief, language, and modern history created the diversity people experience today.</p><div class="onboarding-points"><span>◈ Start with the origin story</span><span>◷ Real images and sources</span><span>✦ No account required</span></div><a class="primary-button onboarding-start" href="#/class/what-is-indonesia">Start the first class <span>→</span></a><a class="landing-secondary-link" href="#/why-indonesia">See why this knowledge is useful →</a><div class="onboarding-preview"><div><b>Every class follows one simple loop</b><span>Read the story → understand the idea → practice the language → recall what matters</span></div><a href="#/class/what-is-indonesia">Preview class →</a></div><details class="profile-setup"><summary>Personalize your learning (optional)</summary><form class="onboarding-form" data-onboarding-form><label>What should we call you?<input name="name" value="${escapeHtml(state.profile.name === 'Fredrick' ? '' : state.profile.name)}" placeholder="Your name" autocomplete="name"/></label><label>What are you hoping to understand?<textarea name="goal" placeholder="For example: the people and history behind the places I visit">${escapeHtml(state.profile.goal === 'Understand Indonesia region by region' ? '' : state.profile.goal)}</textarea></label><button class="secondary-button" type="submit">Save profile and continue <span>→</span></button></form></details><small class="onboarding-note">Private by design · progress stays on this device</small></main>`;
}
function whyIndonesiaPage() {
  return `<main class="why-page"><header class="why-header"><a class="brand" href="#/" aria-label="Nusantara Learning home"><span class="brand-mark">✦</span><span><strong>Nusantara</strong><small>Learning journey</small></span></a><a class="nav-link" href="#/">Start learning →</a></header><section class="why-hero"><span class="eyebrow">WHY NUSANTARA LEARNING?</span><h1>Knowledge of Indonesia helps you read the world around you.</h1><p>Indonesia is one of the world’s most diverse societies. Understanding how that diversity formed makes everyday conversations, travel, collaboration, and business more thoughtful and more effective.</p><a class="primary-button" href="#/class/what-is-indonesia">Begin with the origin story <span>→</span></a></section><section class="why-grid"><article><span class="why-icon">◌</span><span class="eyebrow">EVERYDAY LIFE</span><h2>Recognize the context behind people and places</h2><p>Understand why language, food, religion, family customs, city life, and regional identity can vary so much—and why one national label never tells the whole story.</p></article><article><span class="why-icon">↗</span><span class="eyebrow">BUSINESS & WORK</span><h2>Build better relationships across regions</h2><p>Indonesia is not one uniform market. Geography, local networks, history, language, and community expectations shape how people communicate, buy, travel, and work together.</p></article><article><span class="why-icon">◎</span><span class="eyebrow">TRAVEL & PLACE</span><h2>See more than attractions</h2><p>A map becomes more useful when you know the histories of ports, kingdoms, migration routes, belief systems, and the communities who give each place meaning.</p></article><article><span class="why-icon">✦</span><span class="eyebrow">BACKGROUND KNOWLEDGE</span><h2>Follow the news with a stronger mental model</h2><p>Learn the long arc from island societies and maritime exchange to independence, Reformasi, regional autonomy, and today’s debates about development and identity.</p></article></section><section class="why-path"><div><span class="eyebrow">THE LEARNING PATH</span><h2>Start at the beginning, then follow the connections.</h2><p>The course moves from geography and early exchange to peoples, languages, kingdoms, belief, independence, and modern Indonesia. Each class gives you a useful idea—not just a flashcard to memorize.</p></div><div class="timeline"><div><b>01</b><span>How island societies became connected</span></div><div><b>02</b><span>How trade and geography shaped regions</span></div><div><b>03</b><span>How migration and history created diversity</span></div><div><b>04</b><span>How one republic holds many identities</span></div></div></section><section class="why-cta"><span class="eyebrow">NO BACKGROUND REQUIRED</span><h2>Begin with one clear story about Indonesia.</h2><p>Read, think, answer, and continue. Your progress stays on this device.</p><a class="primary-button" href="#/class/what-is-indonesia">Start Class 1 <span>→</span></a></section></main>`;
}
function dashboardPage() {
  const progress = calculateProgress(state, LESSONS.length);
  const level = getLevelSummary(state.xp);
  const history = [...state.history].reverse().map((entry) => { const item = findLesson(entry.lessonId); const reviewLabel = entry.remembered ? 'remembered' : 'reviewed for tomorrow'; const activityLabel = entry.type === 'review' ? `Review · ${reviewLabel}${entry.xp ? ` · +${entry.xp} XP` : ''}` : `Class complete · +${entry.xp} XP`; return `<a href="#/class/${item.id}" class="history-row"><span class="history-dot">${entry.type === 'review' ? '↻' : '✓'}</span><span><b>${escapeHtml(item.title)}</b><small>${entry.date} · ${activityLabel}</small></span><span>→</span></a>`; }).join('') || '<p class="empty-state">Your completed classes will appear here.</p>';
  const dueReviews = getDueReviews(state, getToday());
  const reviewQueue = dueReviews.length ? `<section class="dashboard-panel review-panel"><div class="section-heading compact"><div><span class="eyebrow">SPACED REVIEW</span><h2>Keep these fresh</h2></div><span class="section-note">${dueReviews.length} due today</span></div><p class="review-intro">A quick retrieval today helps this idea stick. Open the class if you need a refresher.</p><div class="review-list">${dueReviews.map((id) => { const item = findLesson(id); return `<div class="review-row"><a href="#/class/${item.id}"><b>${escapeHtml(item.title)}</b><small>Review class ↗</small></a><div class="review-actions"><button class="secondary-button" data-review="${item.id}" data-remembered="false">Review tomorrow</button><button class="primary-button" data-review="${item.id}" data-remembered="true">I remembered <span>+5 XP</span></button></div></div>`; }).join('')}</div></section>` : `<section class="dashboard-panel review-panel review-clear"><span class="eyebrow">SPACED REVIEW</span><h2>You’re all caught up.</h2><p>Your next review will appear here after a class becomes due.</p></section>`;
  return shell(`<section class="dashboard-hero"><div class="profile-avatar">${escapeHtml((state.profile.name || 'FY').slice(0, 2).toUpperCase())}</div><div><span class="eyebrow">YOUR LEARNING PROFILE</span><h2>${escapeHtml(state.profile.name)}</h2><p>${escapeHtml(state.profile.goal)}</p></div><div class="dashboard-score"><strong>${state.xp}</strong><span>total XP</span></div></section><section class="level-card"><div><span class="eyebrow">YOUR LEVEL</span><h2>${escapeHtml(level.current.name)}</h2><p>${level.next ? `${level.remainingXp} XP to ${escapeHtml(level.next.name)}` : 'You have reached the highest level in this course.'}</p></div><div class="level-progress"><div class="progress-label"><span>${level.next ? 'Next milestone' : 'Course milestone'}</span><strong>${level.progress}%</strong></div><div class="progress-track" role="progressbar" aria-label="Progress to next level" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${level.progress}"><span style="width:${level.progress}%"></span></div></div></section><section class="dashboard-stats"><article><strong>${state.streak}</strong><span>day streak</span></article><article><strong>${state.completedLessonIds.length}/${LESSONS.length}</strong><span>classes complete</span></article><article><strong>${progress}%</strong><span>course progress</span></article><article><strong>${Object.keys(state.quizBest).length}</strong><span>quiz wins</span></article></section>${reviewQueue}<section class="dashboard-columns"><div class="dashboard-panel"><div class="section-heading compact"><div><span class="eyebrow">LEARNING HISTORY</span><h2>Your path so far</h2></div></div><div class="history-list">${history}</div></div><div class="dashboard-panel profile-panel"><span class="eyebrow">PROFILE SETTINGS</span><h2>Shape your journey</h2><label>Name<input data-profile-name value="${escapeHtml(state.profile.name)}"/></label><label>Learning goal<textarea data-profile-goal>${escapeHtml(state.profile.goal)}</textarea></label><button class="secondary-button" data-action="save-profile">Save profile</button><div class="dashboard-reminder"><b>Reminder</b><span>${state.reminderEnabled ? `On at ${state.reminderTime}` : 'Off'}</span><a href="#/" data-scroll="reminder">Change on home →</a></div></div></section>`, false);
}
function render() {
  const route = getRoute();
  if (route.type !== 'home' && !state.onboardingCompleted) {
    state = normalizeState({ ...state, onboardingCompleted: true });
    saveState();
  }
  document.querySelector('#app').innerHTML = route.type === 'why' ? whyIndonesiaPage() : (!state.onboardingCompleted && route.type === 'home')
    ? onboardingPage()
    : route.type === 'dashboard' ? dashboardPage() : route.type === 'class' ? classPage(route.id) : homePage();
  bindEvents(route);
}
function refreshCompletionButton(lessonId) {
  const item = findLesson(lessonId);
  const checks = learningChecks[lessonId] || { sections: {}, vocab: false, synthesis: false };
  const ready = item.sections.every((_, index) => checks.sections[index]) && checks.vocab && checks.synthesis;
  const button = document.querySelector('[data-action="complete"]');
  const status = document.querySelector('[data-completion-status]');
  if (button) button.disabled = !ready;
  if (status) { status.textContent = ready ? 'All checks complete. You can finish the class.' : `Complete ${item.sections.length} section checks, the vocabulary check, and the synthesis check to finish.`; status.className = `completion-status ${ready ? 'ready' : ''}`; }
}
function bindEvents(route) {
  document.querySelectorAll('[data-locked-lesson]').forEach((link) => link.addEventListener('click', (event) => { event.preventDefault(); const index = LESSONS.findIndex((item) => item.id === link.dataset.lockedLesson); const previous = LESSONS[index - 1]; alert(`This class is locked. Complete “${previous.title}” first to unlock it.`); }));
  document.querySelector('[data-onboarding-form]')?.addEventListener('submit', (event) => {
    event.preventDefault();
    const form = event.currentTarget;
    const name = form.elements.name.value.trim() || 'Learner';
    const goal = form.elements.goal.value.trim() || 'Understand Indonesia region by region';
    if (!name) return;
    state = normalizeState({ ...state, profile: { name, goal }, onboardingCompleted: true });
    saveState();
    render();
  });
  document.querySelector('[data-action="guest"]')?.addEventListener('click', () => {
    state = normalizeState({ ...state, onboardingCompleted: true });
    saveState();
    window.location.hash = '#/class/what-is-indonesia';
  });
  document.querySelectorAll('.lesson-media img[data-fallback]').forEach((image) => image.addEventListener('error', () => {
    if (image.dataset.fallback && image.src !== image.dataset.fallback) image.src = image.dataset.fallback;
  }, { once: true }));
  const completeButton = document.querySelector('[data-action="complete"]');
  if (completeButton) completeButton.addEventListener('click', () => { const checks = learningChecks[route.id]; if (!checks?.synthesis || !checks.vocab || !findLesson(route.id).sections.every((_, index) => checks.sections[index])) return; state = completeLesson(state, route.id, getToday(), LESSONS); saveState(); render(); document.querySelector('#class-content')?.scrollIntoView({ behavior: 'smooth' }); });
  document.querySelectorAll('.section-answer').forEach((button) => button.addEventListener('click', () => { const group = button.dataset.section; const buttons = [...document.querySelectorAll(`.section-answer[data-section="${group}"]`)]; const index = Number(button.dataset.answer); const correct = Number(button.dataset.correct); learningChecks[route.id] ||= { sections: {}, vocab: false, synthesis: false }; learningChecks[route.id].sections[group] = true; buttons.forEach((b) => { b.disabled = true; if (Number(b.dataset.answer) === correct) b.classList.add('correct'); if (b === button && index !== correct) b.classList.add('wrong'); }); const result = button.closest('.section-check').querySelector('.section-result'); result.textContent = index === correct ? 'Correct — you understood the idea. Continue when ready.' : `Not quite. The best answer is ${String.fromCharCode(65 + correct)}.`; result.className = `section-result ${index === correct ? 'success' : 'retry'}`; refreshCompletionButton(route.id); }));
  document.querySelectorAll('.vocab-answer').forEach((button) => button.addEventListener('click', () => { const buttons = [...document.querySelectorAll('.vocab-answer')]; const index = Number(button.dataset.vocabAnswer); const correct = Number(button.dataset.vocabCorrect); learningChecks[route.id] ||= { sections: {}, vocab: false, synthesis: false }; learningChecks[route.id].vocab = true; buttons.forEach((b) => { b.disabled = true; if (Number(b.dataset.vocabAnswer) === correct) b.classList.add('correct'); if (b === button && index !== correct) b.classList.add('wrong'); }); const result = document.querySelector('.vocab-result'); result.textContent = index === correct ? 'Correct — this is the word used in the class.' : `Not quite. The answer is ${String.fromCharCode(65 + correct)}.`; result.className = `vocab-result ${index === correct ? 'success' : 'retry'}`; refreshCompletionButton(route.id); }));
  document.querySelectorAll('#quiz-card [data-answer]').forEach((button) => button.addEventListener('click', () => { const item = findLesson(route.id); const index = Number(button.dataset.answer); const correct = index === item.correct; learningChecks[route.id] ||= { sections: {}, vocab: false, synthesis: false }; learningChecks[route.id].synthesis = true; state = markAnswer(state, item.id, correct); saveState(); document.querySelectorAll('#quiz-card [data-answer]').forEach((b, i) => { b.disabled = true; if (i === item.correct) b.classList.add('correct'); if (i === index && !correct) b.classList.add('wrong'); }); const result = document.querySelector('#quiz-card .quiz-result'); result.textContent = correct ? 'Correct — +10 XP. You found the thread.' : `Not quite. The answer is ${String.fromCharCode(65 + item.correct)}.`; result.className = `quiz-result ${correct ? 'success' : 'retry'}`; refreshCompletionButton(route.id); }));
  document.querySelectorAll('[data-review]').forEach((button) => button.addEventListener('click', () => { state = reviewLesson(state, button.dataset.review, getToday(), button.dataset.remembered === 'true'); saveState(); render(); }));
  document.querySelector('[data-note]')?.addEventListener('input', (event) => { state.notes[route.id] = event.target.value; saveState(); });
  document.querySelector('[data-reminder-toggle]')?.addEventListener('change', (event) => { state.reminderEnabled = event.target.checked; saveState(); render(); });
  document.querySelector('[data-reminder-time]')?.addEventListener('change', (event) => { state.reminderTime = event.target.value; saveState(); render(); });
  document.querySelector('[data-action="notifications"]')?.addEventListener('click', enableNotifications);
  document.querySelector('[data-action="save-profile"]')?.addEventListener('click', () => { state.profile.name = document.querySelector('[data-profile-name]').value.trim() || 'Learner'; state.profile.goal = document.querySelector('[data-profile-goal]').value.trim() || 'Understand Indonesia region by region'; saveState(); render(); });
  document.querySelector('[data-scroll="reminder"]')?.addEventListener('click', () => { window.location.hash = '#/'; setTimeout(() => document.querySelector('#reminder')?.scrollIntoView({ behavior: 'smooth' }), 50); });
}
async function enableNotifications() { if (!('Notification' in window)) { alert('Browser notifications are not supported here.'); return; } const permission = await Notification.requestPermission(); if (permission === 'granted') new Notification('Nusantara Learning', { body: `Your next Indonesia class is: ${currentLesson().title}` }); }
window.addEventListener('hashchange', render);
if (!window.location.hash) window.location.hash = '#/';
render();
if (shouldRemindToday(state, getToday())) document.title = 'Nusantara Learning · Your class is waiting';

if ('serviceWorker' in navigator && (window.location.protocol === 'https:' || window.location.hostname === 'localhost')) {
  window.addEventListener('load', () => navigator.serviceWorker.register('/sw.js').catch(() => {}));
}
